import EventsCarousel from '../components/EventsCarousel';
import ProjectList from '../components/ProjectList';

const Home = () => {
    return (
        <div>
            <ProjectList />
            <EventsCarousel />
        </div>
    );
};

export default Home;